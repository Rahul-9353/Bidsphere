server:
  port: 8083

spring:
  application:
    name: bidding-service
  datasource:
    url:  jdbc:postgresql://localhost:5432/bidsphere_bids
    username: postgres
    password: Rahul@24
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka/
  instance:
    prefer-ip-address:  true